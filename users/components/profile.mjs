import React from 'react';
import Calendar from 'react-calendar';

const Profile = () => {
  return (
    <div>
      <h1>Profile</h1>
      <Calendar />
      {/* Add form for flavor and topping selection */}
    </div>
  );
};

export default Profile;